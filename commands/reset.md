---
description: Reset learned preferences for one vibe or all of them.
argument-hint: "[all|vibe-slug]"
allowed-tools: mcp__cadence__reset
---

The user invoked `/cadence:reset` with arguments: `$ARGUMENTS`

Call `mcp__cadence__reset` with `target` set to `all` or a vibe slug (e.g. `deep-focus`). If no argument is given, ask the user whether they mean `all` before proceeding. Report the result.
