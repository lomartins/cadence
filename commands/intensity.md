---
description: Set energy intensity for the current vibe (0 minimal … 4 peak).
argument-hint: "[0-4]"
allowed-tools: mcp__cadence__set_intensity
---

The user invoked `/cadence:intensity` with arguments: `$ARGUMENTS`

Call `mcp__cadence__set_intensity` with `intensity` set to the number given (0-4). Report the result.
