---
description: Turn automatic work-mode music switching on, off, or toggle it.
argument-hint: "[on|off|toggle]"
allowed-tools: mcp__cadence__auto
---

The user invoked `/cadence:auto` with arguments: `$ARGUMENTS`

Call `mcp__cadence__auto` with `mode` set to `on`, `off`, or `toggle` (default `toggle` if no argument). Report the new state.
